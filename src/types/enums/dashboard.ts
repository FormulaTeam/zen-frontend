export enum IDashboardStatic {
  TOTAL_FORMS = "totalForms",
  ACTIVE_FORMS = "activeForms",
  INACTIVE_FORMS = "inactiveForms",
  ZERO_COMMENTS = "zeroComments",
  DAILY_USERS = "dailyUsers",
  MONTHLY_USERS = "monthlyUsers",
  FORMS_BY_MONTH = "formsByMonth",
}

export enum MonthOfYear {
  JAN = 1,
  FEB = 2,
  MAR = 3,
  APR = 4,
  MAY = 5,
  JUN = 6,
  JUL = 7,
  AUG = 8,
  SEP = 9,
  OCT = 10,
  NOV = 11,
  DEC = 12,
}

export enum IRetrieveDataType {
  CREATED = "created",
  DELETED = "deleted",
  UNITS = "units",
}


export enum IChartType {
  PIE = "pie",
  COLUMN = "column",
}
